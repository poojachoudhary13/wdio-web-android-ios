const appPackage = browser.capabilities.appPackage;
import { testData } from "../../../test-data/app"

export const BasketScreenSelectors = {
    count: `//android.widget.TextSwitcher/android.widget.TextView`,



};

//`android=new UiSelector().resourceId("${appPackage}:id/smallLabel").text("Search")`